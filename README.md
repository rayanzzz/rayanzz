# rayanzz utilities

This repository now contains a small collection of utility code.  The flagship
component is a compact yet fully featured Least Recently Used cache exposed via
`rayanzz.LRUCache`.

## Running the tests

The test suite uses `pytest`.  After installing the dependencies (only `pytest`
is required) you can execute the tests with:

```bash
PYTHONPATH=src pytest
```
